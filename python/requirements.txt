# Core requirements
fastapi
uvicorn
python-dotenv
pydantic

# Required LLM libraries
openai
google-generativeai
anthropic
fireworks-ai
groq

# Required LangChain components - use minimal versions
langchain-core==0.1.15
langchain-openai==0.0.5
langchain-anthropic==0.0.8
langchain-fireworks==0.1.3

# Required for React Agent - slim versions
langgraph==0.0.20

# Remove development dependencies and use minimal versions
# Use direct dependencies only when possible